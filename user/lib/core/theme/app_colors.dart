class AppColors {
  // Brand colors
  static const primary = 0xFF0085D0;
  static const primaryDark = 0xFF0085D0;
  static const primaryDeeper = 0xFF0085D0;
  static const primaryLight = 0xFF2997E3;     // Tuned brand blue for dark mode contrast

  // Light Theme Palette
  static const background = 0xFFF8FAFC;      // Slate 50 (clean light background)
  static const surface = 0xFFFFFFFF;         // Pure white surface containers
  static const card = 0xFFFFFFFF;
  static const cardSubtle = 0xFFF1F5F9;      // Slate 100 subtle container fill
  static const border = 0xFFE2E8F0;          // Slate 200 (light border)
  static const textPrimary = 0xFF0F172A;      // Slate 900 (sharp primary text)
  static const textSecondary = 0xFF64748B;    // Slate 500 (balanced subtext)
  static const textMuted = 0xFF94A3B8;        // Slate 400

  // Semantic Status Colors (Light Theme)
  static const success = 0xFF10B981;         // Emerald Green
  static const warning = 0xFFF59E0B;         // Amber Yellow
  static const danger = 0xFFEF4444;          // Rose Red
  static const info = 0xFF0284C7;            // Sky Blue

  // Quick Action card fixed colors (Excluded from dark mode by design)
  static const blueIndigo = 0xFF4F46E5;       // Cobalt/Indigo
  static const blueRoyal = 0xFF2563EB;        // Royal Blue
  static const blueSky = 0xFF0EA5E9;          // Sky Blue
  static const blueTeal = 0xFF0D9488;         // Teal/Aqua

  // Professional Dark Theme Palette (LinkedIn-inspired warm neutral darks)
  static const darkBackground = 0xFF1D2226;   // LinkedIn Base charcoal (#1D2226)
  static const darkSurface = 0xFF24292E;      // LinkedIn Card / Sheet / Surface (#24292E)
  static const darkCard = 0xFF24292E;         // Elevated card surface (#24292E)
  static const darkCardSubtle = 0xFF2D3339;   // Nested containers / inputs / chips (#2D3339)
  static const darkBorder = 0xFF383E45;       // Hairline subtle divider / border (#383E45)
  static const darkTextPrimary = 0xFFE6E9EC;  // Off-white primary text (#E6E9EC)
  static const darkTextSecondary = 0xFF9AA0A6;// Muted gray secondary text (#9AA0A6)
  static const darkTextMuted = 0xFF737A82;    // Low-emphasis captions / timestamps (#737A82)

  // Semantic Status Colors (Dark Theme - tuned for warm dark surface contrast)
  static const darkSuccess = 0xFF34D399;     // Balanced Emerald
  static const darkWarning = 0xFFFBBF24;     // Soft Amber
  static const darkDanger = 0xFFF87171;      // Soft Rose
  static const darkInfo = 0xFF38BDF8;        // Soft Sky Blue
}
